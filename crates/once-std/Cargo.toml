[package]
name = "once-std"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Standard library for the Once language"

[dependencies]
once-hir = { path = "../once-hir" }
thiserror = { workspace = true }
