[package]
name = "once-bounds"
version = "0.1.0"
edition = "2021"

[dependencies]
once-hir = { path = "../once-hir" }
once-ty = { path = "../once-ty" }
once-lex = { path = "../once-lex" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }

