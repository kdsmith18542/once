[package]
name = "once-lockfile"
version = "0.1.0"
edition = "2021"

[dependencies]
once-onceo = { path = "../once-onceo" }
once-ty = { path = "../once-ty" }
once-effects = { path = "../once-effects" }
once-rinf = { path = "../once-rinf" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
thiserror = "1.0"
chrono = { version = "0.4", features = ["serde"] }
sha2 = "0.10"
hex = "0.4"


