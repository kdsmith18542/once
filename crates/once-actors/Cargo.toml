[package]
name = "once-actors"
version = "0.1.0"
edition = "2021"

[dependencies]
once-hir = { path = "../once-hir" }
once-runtime = { path = "../once-runtime" }
once-std = { path = "../once-std" }
thiserror = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.0", features = ["full"] }
crossbeam = "0.8"

