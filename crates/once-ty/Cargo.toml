[package]
name = "once-ty"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Type system for the Once language"

[dependencies]
once-hir = { path = "../once-hir" }
once-lex = { path = "../once-lex" }
once-linear = { path = "../once-linear" }
thiserror = { workspace = true }
